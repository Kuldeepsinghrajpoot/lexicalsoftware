Place each team member's resume PDF here, named to match `resumeUrl` in
`src/data/team.ts` (e.g. `ayush-gupta.pdf`). Files placed here are served
statically and downloadable from each team member's profile page.
